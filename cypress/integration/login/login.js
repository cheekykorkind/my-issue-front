/* eslint-disable no-undef */
describe("test1", () => {
  // beforeEach(() => {

  // });

  it("test1 it", () => {
    // cy.visit(`${Cypress.env("T_BASE_DOMAIN")}/login`);
    cy.visit(`${Cypress.env("T_BASE_DOMAIN")}/login4`);
    // cy.contains("Welcome to Next.js!", { timeout: 20000 })
  });
});
